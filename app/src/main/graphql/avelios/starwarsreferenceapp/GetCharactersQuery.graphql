query GetCharacters($after: String, $first: Int) {
    allPeople(after: $after, first: $first) {
        edges {
            cursor
            node {
                id
                name
                filmConnection {
                    totalCount
                }
            }
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
