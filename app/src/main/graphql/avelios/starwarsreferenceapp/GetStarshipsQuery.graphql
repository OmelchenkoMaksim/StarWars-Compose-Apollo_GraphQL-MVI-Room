query GetStarships($after: String, $first: Int) {
    allStarships(after: $after, first: $first) {
        edges {
            cursor
            node {
                id
                name
                model
            }
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
