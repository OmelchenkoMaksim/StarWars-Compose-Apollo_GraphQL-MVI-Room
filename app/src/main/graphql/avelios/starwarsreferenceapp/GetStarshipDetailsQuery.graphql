query GetStarshipDetails($id: ID!) {
    starship(id: $id) {
        id
        name
        model
        starshipClass
        manufacturers
        length
        crew
        passengers
        maxAtmospheringSpeed
        hyperdriveRating
    }
}
